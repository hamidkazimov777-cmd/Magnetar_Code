node_modules
dist
.next
out
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo
coverage

# The search index is a local cache. Everything else under .magnetar/ —
# remembered facts and approved commands — is meant to be committed and
# reviewed in a diff.
.magnetar/index.json
